version: '3.9'

services:
  app:
    image: ghcr.io/duopus/rundown-editor:latest
    container_name: duopus-rundown-editor
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=3010
      - DATA_DIR=/app/data
      - NODE_ENV=production
      # Required for HTTP-only deployments; omit or set true when behind HTTPS.
      - SESSION_COOKIE_SECURE=false
    ports:
      - '3010:3010'
    volumes:
      - ./data:/app/data
    restart: unless-stopped
